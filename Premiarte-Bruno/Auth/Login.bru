meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "premiartelp@gmail.com",
    "password": "Premiarte1846@"
  }
}

script:post-response {
  bru.setVar('access_token',res.body.access_token)
}

settings {
  encodeUrl: true
  timeout: 0
}
