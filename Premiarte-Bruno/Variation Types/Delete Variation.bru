meta {
  name: Delete Variation
  type: http
  seq: 4
}

delete {
  url: {{url}}/variation-types/1
  body: none
  auth: inherit
}

script:pre-request {
  req.setHeader("Authorization", "Bearer {{access_token}}");
}

settings {
  encodeUrl: true
  timeout: 0
}
