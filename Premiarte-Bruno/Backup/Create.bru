meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{url}}/backup/run
  body: none
  auth: inherit
}

script:pre-request {
  req.setHeader("Authorization", "Bearer {{access_token}}");
}

settings {
  encodeUrl: true
  timeout: 0
}
